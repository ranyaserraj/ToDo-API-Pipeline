pipeline {
    agent any
    
    environment {
        IMAGE_NAME = 'todo-api'
        CONTAINER_NAME = 'todo-api-jenkins'
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo '📥 Récupération du code source depuis GitHub...'
                checkout scm
            }
        }
        
        stage('Verify Environment') {
            steps {
                echo '🔧 Vérification de l\'environnement...'
                sh '''
                    echo "=== VÉRIFICATION DES OUTILS ==="
                    echo "Git version:"
                    git --version
                    echo "Répertoire de travail:"
                    pwd
                    echo "Contenu du répertoire:"
                    ls -la
                    echo "=== FIN VÉRIFICATION ==="
                '''
            }
        }
        
        stage('Build Docker Image (External)') {
            steps {
                echo '🐳 Construction de l\'image Docker (externe)...'
                script {
                    sh '''
                        echo "Construction de l'image Docker depuis l'extérieur..."
                        echo "Cette étape sera exécutée manuellement après le pipeline"
                        echo "Commandes à exécuter:"
                        echo "docker build -t ${IMAGE_NAME}:${BUILD_NUMBER} ."
                        echo "docker tag ${IMAGE_NAME}:${BUILD_NUMBER} ${IMAGE_NAME}:latest"
                    '''
                }
            }
        }
        
        stage('Run Docker Container (External)') {
            steps {
                echo '🚀 Démarrage du conteneur Docker (externe)...'
                script {
                    sh '''
                        echo "Démarrage du conteneur depuis l'extérieur..."
                        echo "Cette étape sera exécutée manuellement après le pipeline"
                        echo "Commandes à exécuter:"
                        echo "docker stop ${CONTAINER_NAME} || true"
                        echo "docker rm ${CONTAINER_NAME} || true"
                        echo "docker run -d --name ${CONTAINER_NAME} -p 3000:3000 ${IMAGE_NAME}:latest"
                    '''
                }
            }
        }
        
        stage('Health Check (External)') {
            steps {
                echo '🏥 Vérification de la santé de l\'application (externe)...'
                script {
                    sh '''
                        echo "Vérification de l'application..."
                        echo "Cette étape sera exécutée manuellement après le pipeline"
                        echo "Commandes à exécuter:"
                        echo "curl -f http://localhost:3000/health"
                        echo "curl -f http://localhost:3000/tasks"
                    '''
                }
            }
        }
        
        stage('Show Instructions') {
            steps {
                echo '📋 Instructions pour l\'exécution manuelle...'
                script {
                    sh '''
                        echo "=== INSTRUCTIONS POUR L'EXÉCUTION MANUELLE ==="
                        echo "1. Construire l'image Docker:"
                        echo "   docker build -t ${IMAGE_NAME}:${BUILD_NUMBER} ."
                        echo ""
                        echo "2. Démarrer le conteneur:"
                        echo "   docker run -d --name ${CONTAINER_NAME} -p 3000:3000 ${IMAGE_NAME}:latest"
                        echo ""
                        echo "3. Tester l'application:"
                        echo "   curl http://localhost:3000/health"
                        echo "   curl http://localhost:3000/tasks"
                        echo ""
                        echo "4. Voir les logs:"
                        echo "   docker logs ${CONTAINER_NAME}"
                        echo ""
                        echo "5. Arrêter le conteneur:"
                        echo "   docker stop ${CONTAINER_NAME}"
                        echo "   docker rm ${CONTAINER_NAME}"
                        echo ""
                        echo "=== FIN INSTRUCTIONS ==="
                    '''
                }
            }
        }
    }
    
    post {
        always {
            echo '🧹 Nettoyage des ressources...'
            sh '''
                echo "Nettoyage des ressources..."
                echo "Conteneur: ${CONTAINER_NAME}"
                echo "Image: ${IMAGE_NAME}:${BUILD_NUMBER}"
            '''
        }
        success {
            echo '✅ Pipeline réussi !'
            echo '📋 Suivez les instructions ci-dessus pour exécuter Docker manuellement'
            echo '🌐 Application sera disponible sur : http://localhost:3000'
        }
        failure {
            echo '❌ Pipeline échoué !'
            echo '🔍 Vérifiez les logs pour plus de détails'
        }
    }
}
