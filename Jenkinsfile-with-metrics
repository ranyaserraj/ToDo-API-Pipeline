pipeline {
    agent any

    environment {
        NODE_VERSION = '18'
        IMAGE_NAME = 'todo-api'
        APP_PORT = '3000'
        METRICS_COLLECTOR = 'collect-real-metrics.js'
    }

    stages {
        stage('Checkout') {
            steps {
                echo '📥 Récupération du code source...'
                git branch: 'main', credentialsId: 'github-credentials', url: 'https://github.com/ranyaserraj/ToDo-API-Pipeline.git'
            }
        }

        stage('Setup Environment') {
            steps {
                echo '🔧 Configuration de l\'environnement...'
                sh '''
                    echo "Node.js version:"
                    node --version
                    echo "NPM version:"
                    npm --version
                    echo "Docker version:"
                    docker --version
                '''
            }
        }

        stage('Install Dependencies') {
            steps {
                echo '📦 Installation des dépendances...'
                sh 'npm install'
            }
        }

        stage('Unit Tests') {
            steps {
                echo '🧪 Exécution des tests unitaires...'
                sh 'npm run test:unit'
            }
        }

        stage('Integration Tests') {
            steps {
                echo '🌐 Exécution des tests d\'intégration...'
                sh 'npm run test:integration'
            }
        }

        stage('Performance Tests (Heavy)') {
            steps {
                echo '🔥 Exécution des tests de performance LOURDS...'
                sh 'npm run test:performance'
            }
        }

        stage('Collect Real Metrics') {
            steps {
                echo '📊 Collecte des métriques réelles d\'empreinte carbone...'
                script {
                    // Démarrer la collecte des métriques en arrière-plan
                    sh "node ${METRICS_COLLECTOR} &"
                    
                    // Attendre un peu pour la collecte
                    sh "sleep 5"
                    
                    // Vérifier que le rapport a été généré
                    sh "ls -la real-metrics-report.json"
                    
                    echo "Métriques collectées avec succès !"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Construction de l\'image Docker...'
                script {
                    sh "docker build -t ${IMAGE_NAME}:${BUILD_NUMBER} ."
                    sh "docker tag ${IMAGE_NAME}:${BUILD_NUMBER} ${IMAGE_NAME}:latest"
                }
            }
        }

        stage('Run Docker Container') {
            steps {
                echo '🚀 Démarrage du conteneur Docker...'
                script {
                    sh "docker stop ${IMAGE_NAME}-jenkins || true"
                    sh "docker rm ${IMAGE_NAME}-jenkins || true"
                    sh "docker run -d --name ${IMAGE_NAME}-jenkins -p ${APP_PORT}:${APP_PORT} ${IMAGE_NAME}:latest"
                    echo "Conteneur démarré sur http://localhost:${APP_PORT}"
                }
            }
        }

        stage('Health Check & API Tests') {
            steps {
                echo '💖 Vérification de la santé de l\'application...'
                script {
                    sh "sleep 10"
                    sh "curl -s http://localhost:${APP_PORT}/health | grep 'API ToDo est opérationnelle'"
                    sh "curl -s http://localhost:${APP_PORT}/tasks | grep 'success'"
                    echo "Application Docker déployée et API testée avec succès !"
                }
            }
        }

        stage('Update Grafana Dashboard') {
            steps {
                echo '📈 Mise à jour du dashboard Grafana avec les métriques réelles...'
                script {
                    // Vérifier que le rapport de métriques existe
                    sh "test -f real-metrics-report.json"
                    
                    // Copier les métriques vers Grafana
                    sh "cp real-metrics-report.json grafana-metrics.json"
                    
                    // Mettre à jour le dashboard avec les vraies valeurs
                    sh "node update-grafana-dashboard.js"
                    
                    echo "Dashboard Grafana mis à jour avec les métriques réelles !"
                }
            }
        }
    }

    post {
        always {
            echo 'Nettoyage des ressources...'
            sh "docker stop ${IMAGE_NAME}-jenkins || true"
            sh "docker rm ${IMAGE_NAME}-jenkins || true"
        }
        success {
            echo 'Pipeline réussi avec métriques d\'empreinte carbone collectées !'
        }
        failure {
            echo 'Pipeline échoué !'
        }
    }
}
